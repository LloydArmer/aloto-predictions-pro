//
//  ALOTOWidget.swift
//  ALOTOWidget
//
//  Deliberately empty.
//
//  Xcode generated a sample home screen widget here — the one showing a time
//  and an emoji. It was placeholder content of exactly the kind App Review
//  rejects under guideline 2.3.8, so its contents are removed rather than
//  shipped.
//
//  The file itself stays because removing it from the Xcode project needs a
//  Mac, and an empty Swift file compiles to nothing. If a home screen widget
//  is ever wanted, this is where it goes.
//
